# v1.0.0 — Initial stable release

## Changed
- Default format changed to `[%dim:name%]` (with brackets)

## Removed
- Chat dimension display removed. The mod now only shows dimensions in the tab list.
