<h2>v1.0.4 - Fix archive verification rejection</h2>

<h3>Fixed</h3>
<ul>
<li><strong>CurseForge rejected 1.0.3</strong> ("Failed to verify archive"): an unresolved template file was being shipped inside an extra <code>templates/</code> top-level folder in the jar, alongside the real mod metadata. Removed from the final build.</li>
<li><strong>Stale branding</strong>: removed leftover references to the mod's old name ("Dimension TAB") and old command (<code>/dimensiontab</code>) from the mod metadata; the correct command is <code>/infotab</code>.</li>
</ul>

<blockquote>No functional changes since 1.0.2/1.0.3 — this is a packaging fix.</blockquote>
