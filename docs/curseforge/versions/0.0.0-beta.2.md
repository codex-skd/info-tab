<h2>v0.0.0-beta.2 - mod_id normalization</h2>

<blockquote><strong>Breaking change</strong>: the internal mod identifier changed from <code>infotab</code> to <code>info_tab</code>, to match the project's naming convention. The in-game command stays <code>/infotab</code>.</blockquote>

<h3>Changed</h3>
<ul>
<li>Mod identifier normalized: <code>infotab</code> → <code>info_tab</code> (network channel, asset namespace, JAR name).</li>
</ul>
