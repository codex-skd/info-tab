# v1.0.1 — Documentation and workflow update

## Changed
- Updated WORKFLOW.md with new branch structure (`minecraft/<mc-version>/neoforge-<neo-version>/production`)
- Changed commit language to English per project language rules
- Fixed tag format to `<mc-version>-neoforge-<version>` in release steps
