<h2>v1.0.2-beta.7 - Fix AFK never triggering in tab list</h2>

<h3>Fixed</h3>
<ul>
<li><strong>AFK detection</strong>: <code>isAfk()</code> is now evaluated every 5 seconds via server tick, not only on tab list refresh. The <code>[AFK]</code> prefix now actually appears and logs when triggered.</li>
</ul>
